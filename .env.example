# ============================================
# DDoS Detection ML - Environment Variables
# ============================================

# Database connection (auto-configured in Docker)
DATABASE_URL=postgresql://postgres:postgres@db:5432/ddos_detection
PGHOST=db
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=ddos_detection

# Session secret (change this in production!)
SESSION_SECRET=your-super-secret-key-change-in-production

# Environment
NODE_ENV=production

# Server port (internal)
PORT=5000
